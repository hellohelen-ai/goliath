---
title: Goliath
description: An agent harness for on-device language models. Built for Apple Foundation Models and a 4,096-token context window.
template: splash
hero:
  tagline: An agent harness for on-device language models. It plans one step at a time, keeps every context small, confirms before it writes, and hands off to the cloud only when the device cannot finish.
  actions:
    - text: Get started
      link: /goliath/start/introduction/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/hellohelen-ai/goliath
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

```sh
npm i @hellohelen-ai/goliath
```

<CardGrid stagger>
  <Card title="Built for a 4k window" icon="seti:config">
    The conductor sees the ask, a memory brief, and one line per step. Never a raw tool result.
  </Card>
  <Card title="One tool per step" icon="setting">
    Each worker gets a fresh context and one schema. Small models fill flat schemas reliably.
  </Card>
  <Card title="Writes ask first" icon="approve-check">
    Mark a tool `writes: true` and Goliath calls your `confirm` before it runs.
  </Card>
  <Card title="Escalates on structural signals" icon="rocket">
    A repeated call, an empty answer, or the step cap sends the turn to your cloud fallback.
  </Card>
</CardGrid>
